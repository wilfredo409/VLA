import { Respuestaestudiante } from './respuestaestudiante';

describe('Respuestaestudiante', () => {
  it('should create an instance', () => {
    expect(new Respuestaestudiante()).toBeTruthy();
  });
});
